# parcel setup for quick threejs prototypes

install locally:<br/>
`yarn install`
or
`npm install`

run locally:<br/>
`yarn dev`
or
`npm run dev`

run with https locally:<br/>
`yarn dev:https`
or
`npm run dev:https`

build<br/>
`yarn build`
or
`npm run build`